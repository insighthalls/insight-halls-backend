# Superseded

The deployment instructions for this project now live in [`DEPLOY.md`](./DEPLOY.md).

The old guide assumed a backend-only Vercel deploy with a separately-hosted frontend.
The project has since been restructured to deploy frontend + backend together on a single Vercel app, which is simpler and avoids CORS issues. See `DEPLOY.md` for the up-to-date steps.
